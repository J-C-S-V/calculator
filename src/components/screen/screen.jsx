import React from 'react';
import './screen.css';

// Another way to define a component. This syntax is used for simple (mas sencillos) components
export const Screen = ({ input }) => <div className="input">{input}</div>;
